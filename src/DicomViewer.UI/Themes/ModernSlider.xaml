<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:DicomViewer.UI.Controls">

    <!-- Thumb 스타일 / Thumb style -->
    <Style x:Key="ModernSliderThumb" TargetType="{x:Type Thumb}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Ellipse x:Name="PART_Thumb"
                             Width="{Binding ThumbSize, RelativeSource={RelativeSource AncestorType=controls:ModernSlider}}"
                             Height="{Binding ThumbSize, RelativeSource={RelativeSource AncestorType=controls:ModernSlider}}"
                             Fill="{Binding ThumbBackground, RelativeSource={RelativeSource AncestorType=controls:ModernSlider}}"
                             Stroke="{Binding ThumbBorderBrush, RelativeSource={RelativeSource AncestorType=controls:ModernSlider}}"
                             StrokeThickness="{Binding ThumbBorderThickness, RelativeSource={RelativeSource AncestorType=controls:ModernSlider}}"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <ScaleTransform x:Name="ThumbScale" ScaleX="1" ScaleY="1" />
                        </Ellipse.RenderTransform>
                        <Ellipse.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.3" />
                        </Ellipse.Effect>
                    </Ellipse>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ThumbScale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="1.2" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ThumbScale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="1.2" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ThumbScale"
                                                         Storyboard.TargetProperty="ScaleX"
                                                         To="1" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ThumbScale"
                                                         Storyboard.TargetProperty="ScaleY"
                                                         To="1" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="PART_Thumb" Property="Fill" Value="#0078D4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Repeat Button (투명) / Repeat Button (transparent) -->
    <Style x:Key="SliderRepeatButtonTransparent" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Rectangle Fill="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Horizontal Slider Template -->
    <ControlTemplate x:Key="HorizontalModernSlider" TargetType="{x:Type controls:ModernSlider}">
        <Grid VerticalAlignment="Center">
            <!-- 배경 트랙 / Background track -->
            <Border x:Name="PART_TrackBackground"
                    Height="{TemplateBinding TrackHeight}"
                    Background="{TemplateBinding TrackBackground}"
                    CornerRadius="2"
                    VerticalAlignment="Center" />

            <Track x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButtonTransparent}"
                                  Command="{x:Static Slider.DecreaseLarge}" />
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButtonTransparent}"
                                  Command="{x:Static Slider.IncreaseLarge}" />
                </Track.IncreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ModernSliderThumb}" />
                </Track.Thumb>
            </Track>

            <!-- 진행 트랙 / Progress track -->
            <Border x:Name="PART_SelectionRange"
                    Height="{TemplateBinding TrackHeight}"
                    Background="{TemplateBinding ProgressFill}"
                    CornerRadius="2"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Width="0" />
        </Grid>
    </ControlTemplate>

    <!-- Vertical Slider Template -->
    <ControlTemplate x:Key="VerticalModernSlider" TargetType="{x:Type controls:ModernSlider}">
        <Grid HorizontalAlignment="Center">
            <!-- 배경 트랙 / Background track -->
            <Border x:Name="PART_TrackBackground"
                    Width="{TemplateBinding TrackHeight}"
                    Background="{TemplateBinding TrackBackground}"
                    CornerRadius="2"
                    HorizontalAlignment="Center" />

            <Track x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButtonTransparent}"
                                  Command="{x:Static Slider.DecreaseLarge}" />
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButtonTransparent}"
                                  Command="{x:Static Slider.IncreaseLarge}" />
                </Track.IncreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ModernSliderThumb}" />
                </Track.Thumb>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style TargetType="{x:Type controls:ModernSlider}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="TrackBackground" Value="#3E3E42" />
        <Setter Property="ProgressFill" Value="#0078D4" />
        <Setter Property="ThumbBackground" Value="#FFFFFF" />
        <Setter Property="ThumbBorderBrush" Value="#0078D4" />
        <Setter Property="ThumbSize" Value="16" />
        <Setter Property="TrackHeight" Value="4" />
        <Setter Property="MinHeight" Value="20" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Template" Value="{StaticResource HorizontalModernSlider}" />
            </Trigger>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Template" Value="{StaticResource VerticalModernSlider}" />
                <Setter Property="MinWidth" Value="20" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
