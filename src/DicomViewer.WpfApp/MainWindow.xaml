<Window x:Class="DicomViewer.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:DicomViewer.ViewModels;assembly=DicomViewer.ViewModels"
        mc:Ignorable="d"
        Title="DICOM Viewer - Medical Imaging Portfolio"
        Height="800" Width="1200"
        MinHeight="600" MinWidth="800"
        WindowState="Maximized"
        Background="#1E1E1E"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 상단 도구 모음 / Top Toolbar -->
        <Border Grid.Row="0" Background="#2D2D30" Padding="8">
            <DockPanel>
                <!-- 왼쪽 버튼들 / Left buttons -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button Content="Open Folder"
                            Command="{Binding CurrentViewModel.OpenFolderCommand}"
                            Style="{StaticResource ToolbarButtonStyle}"
                            Margin="0,0,8,0" />
                    <Button Content="Study List"
                            Command="{Binding NavigateToStudyListCommand}"
                            Style="{StaticResource ToolbarButtonStyle}"
                            Margin="0,0,8,0" />
                    <Button Content="Viewer"
                            Command="{Binding NavigateToViewerCommand}"
                            Style="{StaticResource ToolbarButtonStyle}" />
                </StackPanel>

                <!-- 오른쪽 정보 / Right info -->
                <TextBlock DockPanel.Dock="Right"
                           Text="DICOM Viewer Portfolio v1.0"
                           Foreground="#999999"
                           VerticalAlignment="Center"
                           Margin="8,0" />

                <Border />
            </DockPanel>
        </Border>

        <!-- 메인 콘텐츠 영역 (DataTemplate 매핑) / Main content area (DataTemplate mapping) -->
        <ContentControl Grid.Row="1"
                        Content="{Binding CurrentViewModel}" />

        <!-- 하단 상태 표시줄 / Bottom status bar -->
        <Border Grid.Row="2" Padding="8,4">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#007ACC" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                            <Setter Property="Background" Value="#CA5100" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="White"
                       FontSize="12" />
        </Border>
    </Grid>
</Window>
